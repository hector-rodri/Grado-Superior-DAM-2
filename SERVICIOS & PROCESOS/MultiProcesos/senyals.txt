Senyals:

Amb el seu nom:

kill -ALRM PID # SIGALRM
kill -TERM PID # SIGTERM
kill -INT PID  # SIGINT
kill -HUP PID  # SIGHUP
kill -KILL PID # SIGKILL

Amb el seu número del senyal:

kill -14 PID # SIGALRM
kill -15 PID # SIGTERM
kill -2 PID  # SIGINT
kill -1 PID  # SIGHUP
kill -9 PID  # SIGKILL

